point A = (0, 0, 0)

point B = (2, 0, 0)

point C = (1, 1.732, 0)

point D = (1, 0.577, 1.633)

complex tetrahedron = translate(scale([A, B, C, D], 3), (1, 0))
complex bound = boundary(boundary(tetrahedron))

let tetra = ap("tetrahedron")
let bound = ap("bound")

save "bound" bound
